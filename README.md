# Component Library

A personal collection of useable HTML, CSS and JavaScript componments and templates.

## Purpose
Reuseable building blocks for future web projects.

## Structure
- `base.css` — reset, variables, typography.
- `component.css` — individual components.
- `compenent.js` — interactive components.
- `index.html` — component showcase.